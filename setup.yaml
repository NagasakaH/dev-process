# =============================================================================
# 開発セットアップ設定ファイル（SSOT: Single Source of Truth）
# =============================================================================
# 自動生成元: https://github.com/NagasakaH/opentelemtry/issues/1
# 生成日時: 2026-02-08
# =============================================================================

task_name: "トレース用のライブラリが欲しい"
ticket_id: "1"

# =============================================================================
# 説明（階層化フォーマット）
# =============================================================================
description:
  # 概要
  overview: |
    TracingSampleと横並びで本番運用できるトレース用のライブラリを作成する。
    C#で書かれたdotnet 8で動くコンソールアプリに対して、ログをトレースに埋め込むように
    置き換え、開始終了をトレースできるようにする。

  # 目的
  purpose: |
    - 本番運用可能なトレース用ライブラリの作成
    - OpenTelemetryとJaegerを使用したトレース機能の実現
    - 複数スレッド・async/await環境での親子関係トレースの実現

  # 背景
  background: |
    導入予定のアプリは複数スレッドでモジュールを起動し、モジュールの中でasync awaitを
    使っている環境。
    任意のメソッドで親を設定し、それより後で呼ばれる全てのメソッド（新規タスクや
    並列実行するタスクも含む）で親子関係を設定しOpenTelemetryとJaegerでトレースを
    行えるようにしたい。
    呼び出しメソッドはDIを使っているものもあればstaticなメソッドなども混在している。

  # 要件
  requirements:
    functional:
      - "TracingSampleと横並びで本番運用できるトレース用ライブラリの作成"
      - "保存はJaegerを使用"
      - "C#、dotnet 8で動くコンソールアプリ対応"
      - "ログをトレースに埋め込むように置き換え"
      - "開始終了をトレースできる機能"
      - "複数スレッドでモジュールを起動する環境に対応"
      - "モジュール内でasync awaitを使用する環境に対応"
      - "任意のメソッドで親を設定可能"
      - "新規タスクや並列実行するタスクでも親子関係を設定可能"
      - "DIを使っているメソッドに対応"
      - "staticなメソッドに対応"
      - "様々なパターンで親となるActivityを作成可能"
      - "各メソッドの呼び出しの開始終了を記録"
    non_functional:
      - "本番運用可能な品質"  # ※手動で補完推奨

  # 受け入れ条件
  acceptance_criteria:
    - "スレッドやasyncなどの呼び出しパターンを網羅した要件が洗い出されていること"
    - "各パターンに対するテストパターンが定義されていること"
    - "テストパターンに対する実装方針が検討されていること"

  # スコープ
  scope:
    - "トレース用ライブラリの作成"
    - "呼び出しパターンのバリエーション洗い出し"
    - "テストパターンと実装の検討"

  # スコープ外
  out_of_scope: []  # ※Issue未記載のため手動で補完推奨

  # 補足
  notes: |
    - 可能な限りいろいろなパターンで親となるActivityを作成できるようにする
    - その子要素としていろいろなバリエーションで呼び出される各メソッドの呼び出しの
      開始終了を記録できるライブラリを作成する

# =============================================================================
# リポジトリ設定
# =============================================================================

# 関連リポジトリ（※自動抽出結果 - 必要に応じて編集）
related_repositories:
  - name: "TracingSample"
    url: "https://github.com/NagasakaH/opentelemtry"  # ※TracingSampleの場所を確認して修正

# 修正対象リポジトリ（※自動抽出結果 - 必要に応じて編集）
target_repositories:
  - name: "opentelemtry"
    url: "https://github.com/NagasakaH/opentelemtry"

# オプション設定
options:
  create_design_document: true
  design_document_dir: "docs"
  submodules_dir: "submodules"
