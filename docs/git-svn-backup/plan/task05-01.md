# タスク: task05-01 - CI構成ファイル作成

## タスク情報

| 項目 | 値 |
|------|-----|
| タスク識別子 | task05-01 |
| タスク名 | CI構成ファイル作成（.gitlab-ci.yml） |
| 前提条件タスク | task04 |
| 並列実行可否 | 可（task05-02と並列） |
| 推定所要時間 | 5分 |
| 優先度 | 高 |

---

## 作業環境

- **作業ディレクトリ（worktree）**: /tmp/GIT-SVN-001-task05-01/
- **ブランチ**: GIT-SVN-001-task05-01
- **ターゲットリポジトリ**: submodules/git-svn-backup
- **作業ブランチ**: sync（orphan ブランチ）

---

## 前提条件

### 前提タスク成果物

| タスク | 成果物パス | 参照内容 |
|--------|------------|----------|
| task04 | `sync-to-svn.sh` | 同期スクリプト（sync ジョブから呼び出し） |
| task03 | `e2e-test.sh` | E2Eテストスクリプト（test ジョブから呼び出し） |
| task01 | `compose.yaml` | Docker Compose 定義（e2e-test ジョブで使用） |

### 確認事項

- [ ] task04 が完了していること
- [ ] sync-to-svn.sh, e2e-test.sh, compose.yaml が存在すること
- [ ] 前提タスクのコミットが cherry-pick 済みであること

---

## 作業内容

### 目的

GitLab CI/CD の定期同期ジョブとE2Eテストジョブを定義する .gitlab-ci.yml を作成する。

### 設計参照

- [design/03_data-structure-design.md](../design/03_data-structure-design.md) - .gitlab-ci.yml の完全な定義

### 実装ステップ

1. .gitlab-ci.yml を設計書に基づき作成
2. sync-to-svn ジョブの定義（stage: sync, schedule/web トリガー）
3. e2e-test ジョブの定義（stage: test, merge_request/web トリガー, docker:dind）
4. 変数定義（GIT_DEPTH: 0, GIT_STRATEGY: clone）

### 対象ファイル

| ファイル | 操作 | 説明 |
|----------|------|------|
| `.gitlab-ci.yml` | 新規作成 | GitLab CI ジョブ定義 |

---

## テスト方針（TDD: RED-GREEN-REFACTOR）

### RED: 失敗するテストケース

**目的**: 実装前にファイルが存在しないことを確認し、テストが失敗する状態を記録する

```bash
# 【実装前に実行して失敗を確認すること】
# ファイルが存在しないことを確認
test -f .gitlab-ci.yml && echo "FAIL: already exists" || echo "PASS: not yet created"
```

**完了条件**: 上記コマンドの実行結果（失敗）をログに記録すること

### GREEN: 最小限の実装

ファイル作成後の検証:

```bash
# ファイルが存在すること
test -f .gitlab-ci.yml

# 必須セクションが含まれること
grep -q "sync-to-svn" .gitlab-ci.yml
grep -q "e2e-test" .gitlab-ci.yml
grep -q "GIT_DEPTH: 0" .gitlab-ci.yml

# gitlab-ci-local での構文チェック（利用可能な場合）
gitlab-ci-local --list 2>/dev/null || echo "gitlab-ci-local not available"
```

設計書（03_data-structure-design.md セクション3）の .gitlab-ci.yml をそのまま作成する。

```yaml
stages:
  - sync
  - test

variables:
  GIT_DEPTH: 0
  GIT_STRATEGY: clone

sync-to-svn:
  stage: sync
  image: debian:bookworm-slim
  before_script:
    - apt-get update -qq && apt-get install -y -qq git git-svn subversion > /dev/null
    - |
      YQ_VERSION="v4.44.1"
      wget -qO /usr/local/bin/yq "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64"
      chmod +x /usr/local/bin/yq
  script:
    - ./sync-to-svn.sh
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
    - if: $CI_PIPELINE_SOURCE == "web"

e2e-test:
  stage: test
  image: docker:latest
  services:
    - docker:dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  before_script:
    - apk add --no-cache bash git git-svn subversion perl-git
    - |
      YQ_VERSION="v4.44.1"
      wget -qO /usr/local/bin/yq "https://github.com/mikefarah/yq/releases/download/${YQ_VERSION}/yq_linux_amd64"
      chmod +x /usr/local/bin/yq
  script:
    - docker compose up -d
    - sleep 3
    - ./e2e-test.sh
  after_script:
    - docker compose down -v 2>/dev/null || true
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_PIPELINE_SOURCE == "web"
```

---

## 成果物

### 期待される出力

| 成果物 | パス | 説明 |
|--------|------|------|
| CI構成 | `.gitlab-ci.yml` | GitLab CI ジョブ定義（sync + e2e-test） |

---

## 完了条件

### 機能的条件

- [ ] .gitlab-ci.yml が存在する
- [ ] sync-to-svn ジョブが定義されている（stage: sync, schedule/web）
- [ ] e2e-test ジョブが定義されている（stage: test, merge_request/web）
- [ ] GIT_DEPTH: 0 が設定されている
- [ ] e2e-test ジョブで docker compose up/down が定義されている
- [ ] gitlab-ci-local --list で構文エラーがないこと（利用可能な場合）

### 品質条件

- [ ] 設計書（03_data-structure-design.md セクション3）と一致すること
- [ ] YAML 構文が正しいこと
- [ ] RED実行証跡（実装前のテスト失敗ログ）が記録されていること

### 設計逸脱の記録

設計書（03_data-structure-design.md）からの変更点がある場合は、以下に明示的に記録すること:

- before_script の snap 分岐削除 → wget 方式に統一（CI環境でsnapが使えないため）
- その他、実装時に設計書から乖離した箇所があれば理由とともに記録する

---

## コミット

```bash
cd /tmp/GIT-SVN-001-task05-01/
git add .gitlab-ci.yml
git commit -m "task05-01: CI構成ファイル作成

- .gitlab-ci.yml: sync-to-svn + e2e-test ジョブ定義
- sync-to-svn: schedule/web トリガー、debian:bookworm-slim
- e2e-test: merge_request/web トリガー、docker:dind"
```

---

## 注意事項

- 設計書の .gitlab-ci.yml をそのまま使用する（独自変更を加えない）
- before_script の snap 分岐は削除し、wget 方式に統一する（CI環境ではsnapが使えないため）
- gitlab-ci-local での注意事項コメントは .gitlab-ci.yml 内には含めない（README.md に記載）
