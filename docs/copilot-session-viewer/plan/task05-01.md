# タスク: task05-01 - useTerminalWebSocket フック

## タスク情報

| 項目 | 値 |
|------|-----|
| タスク識別子 | task05-01 |
| タスク名 | useTerminalWebSocket フック |
| 前提条件タスク | task03-01 |
| 並列実行可否 | 可（task03-02 と並列） |
| 推定所要時間 | 15分 |

## 作業環境

- **作業ディレクトリ（worktree）**: submodules/editable/copilot-session-viewer
- **ブランチ**: feature/tmux-pane-viewer

## 前提条件

- task03-01 完了（型定義が利用可能）
- task01 完了（依存パッケージインストール済み）

## 作業内容

### 目的

`src/hooks/useTerminalWebSocket.ts` を新規作成し、WebSocket 接続の確立・メッセージ送受信・再接続・切断を管理する React フックを実装する。

### 設計参照

- `02_interface-api-design.md` §3.3 useTerminalWebSocket
- `03_data-structure-design.md` §1.5 フロントエンド状態型
- `04_process-flow-design.md` §2 状態遷移図
- `02_interface-api-design.md` §5.2 再接続ロジック

### 実装ステップ

1. **hooks ディレクトリ作成**
   ```bash
   mkdir -p src/hooks
   ```

2. **型の再利用**
   - ws-terminal.ts からフロントエンド用の型を分離（ConnectionState, UseTerminalWebSocketReturn, UseTerminalWebSocketOptions）
   - または共有型ファイルを作成

3. **useTerminalWebSocket フック実装**
   - `useState` で connectionState 管理
   - `useRef` で WebSocket インスタンス管理
   - `useEffect` で接続のライフサイクル管理
   - WebSocket URL 構築: `ws://${location.host}/ws/terminal?sessionId=${sessionId}`
   - メッセージハンドラー:
     - `connected` → connectionState = "connected", onConnected
     - `output` → onOutput
     - `error` → connectionState = "error", onError
     - `disconnected` → connectionState = "disconnected", onDisconnected
     - `pong` → KeepAlive 応答受信
   - 再接続ロジック: 1秒→2秒→4秒（指数バックオフ、最大3回）
   - `sendInput`: WebSocket 経由で InputMessage 送信
   - `sendResize`: WebSocket 経由で ResizeMessage 送信
   - `disconnect`: WebSocket close + クリーンアップ

4. **KeepAlive ping**
   - 30秒間隔で `{ type: "ping" }` 送信
   - pong 応答タイムアウト（15秒）で再接続

### 対象ファイル

| ファイル | 操作 | 変更内容 |
|----------|------|----------|
| `src/hooks/useTerminalWebSocket.ts` | 新規 | WebSocket 接続管理フック |

## テスト方針（TDD: RED-GREEN-REFACTOR）

### RED: 失敗するテストケース

```typescript
// src/hooks/__tests__/useTerminalWebSocket.test.ts

import { renderHook, act, waitFor } from "@testing-library/react";
import { useTerminalWebSocket } from "../useTerminalWebSocket";

// WebSocket をモック
class MockWebSocket {
  static OPEN = 1;
  static CLOSED = 3;
  readyState = MockWebSocket.OPEN;
  url: string;
  onopen?: () => void;
  onmessage?: (event: any) => void;
  onclose?: (event: any) => void;
  onerror?: (event: any) => void;
  send = vi.fn();
  close = vi.fn(() => {
    this.readyState = MockWebSocket.CLOSED;
    this.onclose?.({ code: 1000, reason: "normal" });
  });
  addEventListener = vi.fn();
  removeEventListener = vi.fn();

  constructor(url: string) {
    this.url = url;
    // 非同期で onopen を呼び出し（実際の WebSocket 挙動をシミュレート）
    setTimeout(() => this.onopen?.(), 0);
  }
}

let mockWsInstance: MockWebSocket;
vi.stubGlobal("WebSocket", class extends MockWebSocket {
  constructor(url: string) {
    super(url);
    mockWsInstance = this;
  }
});

describe("useTerminalWebSocket", () => {
  beforeEach(() => {
    vi.useFakeTimers();
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  // UT-29: sessionId null → 未接続
  it("sessionId が null の場合は接続しない", () => {
    const { result } = renderHook(() => useTerminalWebSocket(null));
    expect(result.current.connectionState).toBe("disconnected");
  });

  // UT-30: sessionId 設定 → connecting → connected 状態遷移
  it("sessionId が設定されると connecting → connected message 受信後 connected 状態になる", async () => {
    const onConnected = vi.fn();
    const { result } = renderHook(() =>
      useTerminalWebSocket("session-001", { onConnected })
    );
    expect(result.current.connectionState).toBe("connecting");

    // サーバーからの connected メッセージをシミュレート
    act(() => {
      mockWsInstance.onmessage?.({
        data: JSON.stringify({ type: "connected", sessionId: "session-001", paneSize: { cols: 80, rows: 24 } }),
      });
    });

    expect(result.current.connectionState).toBe("connected");
    expect(onConnected).toHaveBeenCalledWith(
      expect.objectContaining({ type: "connected", sessionId: "session-001" })
    );
  });

  // UT-31: 再接続ロジック（指数バックオフ 1s→2s→4s、最大3回）
  it("切断後に指数バックオフで再接続を試行する（最大3回）", async () => {
    const { result } = renderHook(() => useTerminalWebSocket("session-001"));

    // connected 状態にする
    act(() => {
      mockWsInstance.onmessage?.({
        data: JSON.stringify({ type: "connected", sessionId: "session-001", paneSize: { cols: 80, rows: 24 } }),
      });
    });
    expect(result.current.connectionState).toBe("connected");

    // 切断をシミュレート（異常切断 code: 1006）
    act(() => {
      mockWsInstance.readyState = MockWebSocket.CLOSED;
      mockWsInstance.onclose?.({ code: 1006, reason: "abnormal" });
    });
    expect(result.current.connectionState).toBe("reconnecting");

    // 1秒後に1回目の再接続
    act(() => { vi.advanceTimersByTime(1000); });
    // 2秒後に2回目の再接続
    act(() => {
      mockWsInstance.onclose?.({ code: 1006, reason: "abnormal" });
      vi.advanceTimersByTime(2000);
    });
    // 4秒後に3回目の再接続
    act(() => {
      mockWsInstance.onclose?.({ code: 1006, reason: "abnormal" });
      vi.advanceTimersByTime(4000);
    });
    // 3回失敗後は disconnected 状態になる
    act(() => {
      mockWsInstance.onclose?.({ code: 1006, reason: "abnormal" });
    });
    expect(result.current.connectionState).toBe("disconnected");
  });

  // UT-32: KeepAlive ping 送信と pong タイムアウト
  it("30秒間隔で ping を送信し、15秒以内に pong がなければ再接続する", () => {
    const { result } = renderHook(() => useTerminalWebSocket("session-001"));

    // connected 状態にする
    act(() => {
      mockWsInstance.onmessage?.({
        data: JSON.stringify({ type: "connected", sessionId: "session-001", paneSize: { cols: 80, rows: 24 } }),
      });
    });

    // 30秒経過 → ping 送信
    act(() => { vi.advanceTimersByTime(30000); });
    expect(mockWsInstance.send).toHaveBeenCalledWith(
      JSON.stringify({ type: "ping" })
    );

    // pong 応答を受信 → 接続維持
    act(() => {
      mockWsInstance.onmessage?.({
        data: JSON.stringify({ type: "pong" }),
      });
    });
    expect(result.current.connectionState).toBe("connected");

    // 次の ping 送信後、pong なしで 15 秒タイムアウト → 再接続
    act(() => { vi.advanceTimersByTime(30000); });
    act(() => { vi.advanceTimersByTime(15000); });
    expect(result.current.connectionState).toBe("reconnecting");
  });

  // UT-33: error メッセージで error 状態遷移
  it("error メッセージ受信で error 状態に遷移する", () => {
    const onError = vi.fn();
    const { result } = renderHook(() =>
      useTerminalWebSocket("session-001", { onError })
    );

    act(() => {
      mockWsInstance.onmessage?.({
        data: JSON.stringify({ type: "error", code: "SESSION_NOT_FOUND", message: "Session not found" }),
      });
    });

    expect(result.current.connectionState).toBe("error");
    expect(result.current.error).toBe("Session not found");
    expect(onError).toHaveBeenCalled();
  });
});
```

### GREEN: 最小限の実装

```typescript
// src/hooks/useTerminalWebSocket.ts

import { useState, useRef, useEffect, useCallback } from "react";
import type { ConnectedMessage, ErrorMessage } from "../lib/ws-terminal";

export type ConnectionState = "connecting" | "connected" | "reconnecting" | "disconnected" | "error";

export interface UseTerminalWebSocketOptions {
  onOutput?: (data: string) => void;
  onConnected?: (info: ConnectedMessage) => void;
  onError?: (error: ErrorMessage) => void;
  onDisconnected?: (reason: string) => void;
}

export function useTerminalWebSocket(
  sessionId: string | null,
  options?: UseTerminalWebSocketOptions
) {
  const [connectionState, setConnectionState] = useState<ConnectionState>("disconnected");
  const [error, setError] = useState<string | null>(null);
  const wsRef = useRef<WebSocket | null>(null);
  const reconnectCountRef = useRef(0);

  const connect = useCallback(() => {
    if (!sessionId) return;
    const ws = new WebSocket(`ws://${location.host}/ws/terminal?sessionId=${sessionId}`);
    wsRef.current = ws;
    setConnectionState("connecting");

    ws.onopen = () => { /* wait for connected message */ };
    ws.onmessage = (event) => {
      const msg = JSON.parse(event.data);
      switch (msg.type) {
        case "connected": setConnectionState("connected"); options?.onConnected?.(msg); break;
        case "output": options?.onOutput?.(msg.data); break;
        case "error": setConnectionState("error"); setError(msg.message); options?.onError?.(msg); break;
        case "disconnected": setConnectionState("disconnected"); options?.onDisconnected?.(msg.reason); break;
        case "pong": break;
      }
    };
    ws.onclose = () => { /* reconnect logic */ };
  }, [sessionId, options]);

  useEffect(() => { connect(); return () => wsRef.current?.close(); }, [connect]);

  const sendInput = useCallback((data: string) => {
    wsRef.current?.send(JSON.stringify({ type: "input", data }));
  }, []);

  const sendResize = useCallback((cols: number, rows: number) => {
    wsRef.current?.send(JSON.stringify({ type: "resize", cols, rows }));
  }, []);

  const disconnect = useCallback(() => {
    wsRef.current?.close();
    setConnectionState("disconnected");
  }, []);

  return { connectionState, error, sendInput, sendResize, disconnect };
}
```

### REFACTOR: コード改善

- 再接続ロジックの指数バックオフ実装
- KeepAlive ping/pong の実装
- メモリリーク防止（useEffect クリーンアップ）

## 完了条件

- [ ] `src/hooks/useTerminalWebSocket.ts` が作成されている
- [ ] `src/hooks/__tests__/useTerminalWebSocket.test.ts` が作成され UT-29〜33 が全通過
- [ ] ConnectionState の全状態遷移が実装されている
- [ ] 再接続ロジック（1秒→2秒→4秒、最大3回）が実装
- [ ] KeepAlive ping/pong（30秒間隔、15秒タイムアウト）が実装
- [ ] sendInput / sendResize が WebSocket 経由でメッセージ送信
- [ ] disconnect で WebSocket をクローズしリソースクリーンアップ
- [ ] `npx tsc --noEmit` がエラーなし
