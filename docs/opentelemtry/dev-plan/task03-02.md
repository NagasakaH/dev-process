# タスク: task03-02 - ActivityContextExtensions実装

## タスク情報

| 項目 | 値 |
|------|-----|
| タスク識別子 | task03-02 |
| タスク名 | ActivityContextExtensions実装 |
| 前提条件タスク | task02 |
| 並列実行可否 | 可（task03-01と並列） |
| 推定所要時間 | 1.5時間 |
| 優先度 | 中 |

---

## 作業環境

- **作業ディレクトリ（worktree）**: /tmp/opentelemetry-issue-1-task03-02/
- **ブランチ**: opentelemetry-issue-1-task03-02
- **対象リポジトリ**: TracingSample
- **重要**: 必ず上記の作業ディレクトリ内で作業を行ってください

---

## 前提条件

### 前提タスク成果物

| タスク | 成果物パス | 参照内容 |
|--------|------------|----------|
| task02 | `src/TracingSample.Tracing/Helpers/TraceContext.cs` | TraceContext.Restore() |
| task01 | `src/TracingSample.Tracing/Internal/NoOpScope.cs` | NoOpScope |

### 確認事項

- [ ] task02が完了していること
- [ ] TraceContextが実装されていること
- [ ] task02のコミットがcherry-pick済みであること

---

## 作業内容

### 目的

ActivityContextの操作を簡潔にする拡張メソッドを実装する。Fluent APIスタイルでコンテキスト操作を可能にする。

### 設計参照

- [dev-design/02_interface-api-design.md](../dev-design/02_interface-api-design.md) - 3.4 ActivityContextExtensions クラス

### 実装ステップ

1. **ActivityContextExtensions 作成**
   - AsParent() 拡張メソッド
   - CaptureContext() 拡張メソッド
   - IsValid() 拡張メソッド

2. **ActivityExtensions 作成（追加）**
   - Activity用の便利な拡張メソッド

3. **単体テスト実装**

### 対象ファイル

| ファイル | 操作 | 説明 |
|----------|------|------|
| `src/TracingSample.Tracing/Extensions/ActivityContextExtensions.cs` | 新規作成 | コンテキスト拡張 |
| `tests/TracingSample.Tracing.Tests/Unit/ActivityContextExtensionsTests.cs` | 新規作成 | 単体テスト |

---

## テスト方針（TDD: RED-GREEN-REFACTOR）

### RED: 失敗するテストケース

**テストファイル**: `tests/TracingSample.Tracing.Tests/Unit/ActivityContextExtensionsTests.cs`

```csharp
namespace TracingSample.Tracing.Tests.Unit;

public class ActivityContextExtensionsTests : IDisposable
{
    private readonly ActivitySource _activitySource;
    private readonly ActivityListener _listener;
    private readonly List<Activity> _activities = new();

    public ActivityContextExtensionsTests()
    {
        _activitySource = new ActivitySource("ExtensionsTest");
        TraceContext.DefaultActivitySource = _activitySource;
        
        _listener = new ActivityListener
        {
            ShouldListenTo = _ => true,
            Sample = (ref ActivityCreationOptions<ActivityContext> _) => ActivitySamplingResult.AllDataAndRecorded,
            ActivityStarted = a => _activities.Add(a)
        };
        ActivitySource.AddActivityListener(_listener);
    }

    public void Dispose()
    {
        _listener.Dispose();
        _activitySource.Dispose();
        TraceContext.DefaultActivitySource = null;
    }

    [Fact]
    public void AsParent_WithValidContext_RestoresContext()
    {
        // Arrange
        ActivityContext parentContext;
        using (var parent = _activitySource.StartActivity("Parent"))
        {
            parentContext = parent!.Context;
        }

        // Act
        using (parentContext.AsParent())
        {
            using var child = _activitySource.StartActivity("Child");
            
            // Assert
            Assert.Equal(parentContext.TraceId, child!.Context.TraceId);
        }
    }

    [Fact]
    public void AsParent_WithDefaultContext_ReturnsNoOpScope()
    {
        // Arrange
        var defaultContext = default(ActivityContext);

        // Act
        var scope = defaultContext.AsParent();

        // Assert
        Assert.NotNull(scope);
        scope.Dispose(); // Should not throw
    }

    [Fact]
    public void CaptureContext_WithActiveActivity_ReturnsContext()
    {
        // Arrange
        using var activity = _activitySource.StartActivity("Test");

        // Act
        var context = activity.CaptureContext();

        // Assert
        Assert.Equal(activity.Context.TraceId, context.TraceId);
        Assert.Equal(activity.Context.SpanId, context.SpanId);
    }

    [Fact]
    public void CaptureContext_WithNullActivity_ReturnsDefault()
    {
        // Arrange
        Activity? nullActivity = null;

        // Act
        var context = nullActivity.CaptureContext();

        // Assert
        Assert.Equal(default, context);
    }

    [Fact]
    public void IsValid_WithValidContext_ReturnsTrue()
    {
        // Arrange
        using var activity = _activitySource.StartActivity("Test");
        var context = activity!.Context;

        // Act
        var isValid = context.IsValid();

        // Assert
        Assert.True(isValid);
    }

    [Fact]
    public void IsValid_WithDefaultContext_ReturnsFalse()
    {
        // Arrange
        var defaultContext = default(ActivityContext);

        // Act
        var isValid = defaultContext.IsValid();

        // Assert
        Assert.False(isValid);
    }
}
```

**確認コマンド**:

```bash
cd /tmp/opentelemetry-issue-1-task03-02/TracingSample
dotnet test --filter "FullyQualifiedName~ActivityContextExtensionsTests"
# 結果: FAIL
```

---

### GREEN: 最小限の実装

**実装ファイル**: `src/TracingSample.Tracing/Extensions/ActivityContextExtensions.cs`

```csharp
using System.Diagnostics;
using TracingSample.Tracing.Helpers;
using TracingSample.Tracing.Internal;

namespace TracingSample.Tracing.Extensions;

/// <summary>
/// ActivityContext操作用の拡張メソッドを提供します。
/// </summary>
public static class ActivityContextExtensions
{
    /// <summary>
    /// 指定したActivityContextを親として設定します。
    /// </summary>
    /// <param name="context">親コンテキスト</param>
    /// <returns>Dispose時に元のコンテキストに戻すオブジェクト</returns>
    /// <example>
    /// var parentContext = Activity.Current?.Context ?? default;
    /// using (parentContext.AsParent())
    /// {
    ///     // この中のトレースはparentContextを親とする
    /// }
    /// </example>
    public static IDisposable AsParent(this ActivityContext context)
    {
        return TraceContext.Restore(context);
    }

    /// <summary>
    /// 現在のActivityからActivityContextをキャプチャします。
    /// </summary>
    /// <param name="activity">対象のActivity</param>
    /// <returns>キャプチャしたActivityContext</returns>
    public static ActivityContext CaptureContext(this Activity? activity)
    {
        return activity?.Context ?? default;
    }

    /// <summary>
    /// ActivityContextが有効かどうかを確認します。
    /// </summary>
    /// <param name="context">確認するコンテキスト</param>
    /// <returns>有効な場合はtrue</returns>
    public static bool IsValid(this ActivityContext context)
    {
        return context.TraceId != default && context.SpanId != default;
    }
}
```

**確認コマンド**:

```bash
cd /tmp/opentelemetry-issue-1-task03-02/TracingSample
dotnet test --filter "FullyQualifiedName~ActivityContextExtensionsTests"
# 結果: PASS
```

---

### REFACTOR: コード改善

**改善ポイント**:

- [ ] XMLドキュメントの充実
- [ ] 使用例のドキュメント追加

---

## 成果物

| 成果物 | パス | 説明 |
|--------|------|------|
| ActivityContextExtensions.cs | `src/TracingSample.Tracing/Extensions/ActivityContextExtensions.cs` | 拡張メソッド |
| テストコード | `tests/TracingSample.Tracing.Tests/Unit/ActivityContextExtensionsTests.cs` | 単体テスト |
| result.md | `docs/opentelemtry/dev-plan/results/task03-02-result.md` | 結果レポート |

---

## 完了条件

### 機能的条件

- [ ] AsParent() でコンテキストを親として設定できる
- [ ] CaptureContext() でActivityからコンテキストを取得できる
- [ ] IsValid() でコンテキストの有効性を確認できる

### 品質条件

- [ ] 全テストが通過すること
- [ ] null安全であること

---

## コミット

```bash
cd /tmp/opentelemetry-issue-1-task03-02/TracingSample

git add -A
git commit -m "task03-02: ActivityContextExtensions実装

- AsParent: コンテキストを親として設定
- CaptureContext: Activityからコンテキスト取得
- IsValid: コンテキスト有効性確認
- 単体テスト追加"

git rev-parse HEAD
```

---

## 注意事項

- task03-01と並列実行可能だが、Extensionsディレクトリ内のファイルのみ編集すること
- TraceContext.Restore()を内部で活用すること
- 既存のServiceCollectionExtensions.csは変更しないこと
