# 05. リスク・懸念事項（round 2）

## 概要

| 項目 | 内容 |
|------|------|
| ラウンド | 2 |
| レビュー観点 | round2 設計に残存するリスク・将来課題 |

## 1. リスク一覧

| ID | リスク | 重大度 | 対応方針 | 状態 |
|----|--------|--------|----------|------|
| R-1 | UT-11 が回帰必須から漏れることで description 正規化のリグレッションを CI で検出できなくなる | Minor | DR2-001 で `06 §2.2` を更新 | open（round2 で起票） |
| R-2 | E2E-5 が回帰必須から漏れることで localhost 混入を CI で検出できなくなる | Minor | DR2-002 で `06 §2.2` を更新 | open（round2 で起票） |
| R-3 | Function partial 宣言不整合により読者が写経した際にビルド失敗する | Minor | DR2-003 で `02 §3.1` を partial に統一 | open（round2 で起票） |

## 2. Info（任意記録、修正不要）

| ID | 内容 | 推奨アクション |
|----|------|----------------|
| INFO-1 | ASL の Lambda ARN を Terraform `jsonencode` 例で動的参照（`aws_lambda_function.x.arn`）に変更するとより安全 | 実装フェーズで余裕があれば対応、必須ではない |
| INFO-2 | CI イメージで Alpine edge リポジトリ依存を避けると CI の安定性が向上 | 実装フェーズで安定運用が必要なら検討、必須ではない |

## 3. 結論

新規 Critical/Major リスクはなし。Minor 3 件（DR2-001/002/003）の解消で総合判定 conditional → 次ラウンドで approved 見込み。
