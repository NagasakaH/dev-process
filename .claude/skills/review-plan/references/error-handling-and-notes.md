# エラーハンドリング・注意事項

## エラーハンドリング

### タスク計画が見つからない

```
エラー: タスク計画が見つかりません
ディレクトリ: docs/{target}/plan/

タスク計画を作成してからレビューを実施してください。
```

### 設計ドキュメントが見つからない

```
警告: 設計ドキュメントが見つかりません
ディレクトリ: docs/{target}/design/

設計との整合性チェックはスキップされます。
```

## 注意事項

- タスク計画ドキュメントが存在しない場合はエラー終了
- 設計ドキュメントが存在しない場合は警告を出し、設計との整合性チェックをスキップ
- 既存の `review-plan/` ディレクトリがある場合は上書き（再レビュー時）
- 受入基準を計画の妥当性判断基準として参照
- レビューは客観的な基準に基づいて実施し、主観的な判断は避ける
- **再帰ループ**: 差し戻し/条件付き承認 → plan で指摘対応 → review-plan 再レビューを繰り返す

## 関連スキル

- 前提スキル: `design` - 設計
- 前提スキル: `plan` - タスク計画（差し戻し時に再実施）
- 後続スキル: `implement` - 実装（承認後に進行）
- 関連スキル: `code-review` — コードレビュー
